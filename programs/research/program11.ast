load system io.
load system math.

function gcdExtended with (a:%integer, b:%integer) do
  if a == 0 do
    return b,0,1.
  else do
    gcd = math@gcd(a, b).
    return gcd
end

io @println ("gcd("+ a + "," + b + ") = " + gcd).