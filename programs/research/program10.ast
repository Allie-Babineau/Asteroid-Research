load system io.
load system math.

function gcd with (a:%integer, b:%integer) do
  if a == 0 do
    return b.
  let c = math@gcd(a, b).
  return c.
end

io @println ("gcd("+ a + "," + b + ") = " + c).