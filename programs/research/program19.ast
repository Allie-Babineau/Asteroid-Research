load system io.

function isTriangleExists with (a:%integer, b:%integer, c:%integer) do
  if (a != 0 and b != 0 and c != 0 and (a+b+c)==180) do
    if ((a+b) >= c or (b+c) >= a or (a+c) >= b) do
      return "YES".
    else do
      return "NO".
  else do
    return "NO".
end