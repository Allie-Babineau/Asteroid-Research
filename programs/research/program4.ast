load system io.
load system math.

function productPrimeFactors with (n%int) do
  let product = 1.
  let i = 2.
  while (i < n+1) do
    if math@mod(n, i) == 0 do
      let isPrime = 1.
    let j = 2.
    while (j < (i/2 + 1)) do
      if math@mod(i, j) == 0 do
        let isPrime = 0.
        break.
      end
      j = j+1.
    end
    